import 'package:flutter/material.dart';

// This screen is in GitLab

class MapEditor extends StatelessWidget {
  const MapEditor({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Map Editor'),
      ),
    );
  }
}
